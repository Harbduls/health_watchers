import { Router } from 'express';

const router = Router();

// Placeholder — AI routes to be implemented
router.get('/status', (_req, res) => res.json({ status: 'success', data: { ai: 'available' } }));

export default router;
